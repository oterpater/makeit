// Vamos crear una clase `Node` va a recibir como argumento 
// un valor `value` y una referecia `ref`, por defecto la referencia 
// es `undefined` y crea los atributos de la clase.

class Node{
	constructor(value, ref){
		this.value = value;
		this.ref = undefined;
	};



	
};